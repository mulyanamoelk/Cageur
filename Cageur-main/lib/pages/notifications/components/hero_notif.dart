import 'package:flutter/material.dart';

class Hero_Notif extends StatelessWidget {
  const Hero_Notif({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset(
      'assets/images/notif.png',
      width: 300,
    );
  }
}
